# 8.4.3 Állapotmentes DHCPv6-kliens konfigurálása

## Feladat: R3 mint állapotmentes DHCPv6-kliens

Az R3 router kliensként konfigurálva SLAAC-kal hozza létre a GUA-ját, és állapotmentes DHCPv6-tól kapja a DNS/domain adatokat.

---

## Az 5 konfigurálási lépés

### 1. lépés – IPv6-forgalomirányítás engedélyezése

```
R3(config)# ipv6 unicast-routing
```

### 2. lépés – LLA konfigurálása az interfészen

```
R3(config)# interface g0/0/1
R3(config-if)# ipv6 enable
```

### 3. lépés – SLAAC konfigurálása

```
R3(config-if)# ipv6 address autoconfig
```

### 4. lépés – Ellenőrzés: GUA megkapva?

```
R3# show ipv6 interface g0/0/1
GigabitEthernet0/0/1 is up, line protocol is up
  IPv6 is enabled, link-local address is FE80::2
  Global unicast address(es):
    2001:DB8:ACAD:1:...
```

### 5. lépés – DHCPv6-adatok ellenőrzése

```
R3# show ipv6 dhcp interface g0/0/1
GigabitEthernet0/0/1 is in client mode
  Prefix State is IDLE (0)
  Information refresh timer expires in 23:56:08
  Address State is IDLE
  List of known servers:
    Reachable via address: FE80::1
    DUID: ...
    DNS server: 2001:DB8:ACAD:1::254
    Domain name: example.com
```

---

## Navigáció

### Előző

[[8.4.2 Állapotmentes DHCPv6-szerver konfigurálása|8.4.2 – Állapotmentes DHCPv6 szerver]]

### Következő

[[8.4.4 Állapottartó DHCPv6-kiszolgáló konfigurálása|8.4.4 – Állapottartó DHCPv6 szerver konfig]]

### Kapcsolódó jegyzetek

- [[8.4.4 Állapottartó DHCPv6-kiszolgáló konfigurálása]]

### Címkék

#DHCPv6 #állapotmentes #kliens #ipv6-address-autoconfig #SLAAC #Cisco

---
