# 4.3.7 Forgalomirányítás konfigurálása 3. rétegbeli switch-en

## A konfiguráció 5 lépése

1. Irányított port konfigurálása (D1 G0/0/1 → R1 felé)
2. IP-alapú forgalomirányítás engedélyezése
3. Forgalomirányítási protokoll konfigurálása (OSPF)
4. Forgalomirányítási tábla ellenőrzése
5. Kapcsolat ellenőrzése (ping)

---

## 1. lépés: Irányított port konfigurálása

```
D1(config)# interface GigabitEthernet0/0/1
D1(config-if)# description Routed Port Link to R1
D1(config-if)# no switchport
D1(config-if)# ip address 10.10.10.2 255.255.255.0
D1(config-if)# no shutdown
D1(config-if)# exit
```

---

## 2. lépés: IP-alapú forgalomirányítás engedélyezése

```
D1(config)# ip routing
```

---

## 3. lépés: OSPF konfigurálása

```
D1(config)# router ospf 10
D1(config-router)# network 192.168.10.0 0.0.0.255 area 0
D1(config-router)# network 192.168.20.0 0.0.0.255 area 0
D1(config-router)# network 10.10.10.0 0.0.0.3 area 0
D1(config-router)# end
```

> **Rendszer üzenet OSPF szomszédság kialakulásakor:**
> `%OSPF-5-ADJCHG: Process 10, Nbr 10.10.10.1 on GigabitEthernet0/0/1 from LOADING to FULL`

---

## 4. lépés: Forgalomirányítási tábla ellenőrzése

```
D1# show ip route | begin Gateway

Gateway of last resort is not set

C   10.10.10.0/30 is directly connected, GigabitEthernet0/0/1
L   10.10.10.2/32 is directly connected, GigabitEthernet0/0/1
O   10.20.20.0/24 [110/2] via 10.10.10.1, GigabitEthernet0/0/1
C   192.168.10.0/24 is directly connected, Vlan10
L   192.168.10.1/32 is directly connected, Vlan10
C   192.168.20.0/24 is directly connected, Vlan20
L   192.168.20.1/32 is directly connected, Vlan20
```

| Kód | Jelentés |
|---|---|
| **C** | Közvetlenül csatlakoztatott |
| **L** | Helyi cím |
| **O** | OSPF-en tanult útvonal (10.20.20.0/24 elérhetővé vált!) |

---

## 5. lépés: Kapcsolat ellenőrzése (ping a szerverhez)

```
C:\Users\PC1> ping 10.20.20.254

Request timed out.      ← első csomag elveszhet (ARP)
Reply from 10.20.20.254: bytes=32 time<1ms TTL=127
Reply from 10.20.20.254: bytes=32 time<1ms TTL=127
Reply from 10.20.20.254: bytes=32 time<1ms TTL=127

Packets: Sent = 4, Received = 3, Lost = 1 (25% loss).
```

PC1 és PC2 is el tudja érni a szervert az R1-en keresztül.

---

## Navigáció

### Előző

[[4.3.6 A forgalomirányítás esete 3. rétegbeli switch-en|4.3.6 – Forgalomirányítás esete]]

### Következő

[[4.4.1 A VLAN-ok közötti forgalomirányítás gyakori problémái|4.4.1 – Hibaelhárítás: gyakori problémák]]

### Kapcsolódó jegyzetek

- [[4.3.5 Forgalomirányítás 3. rétegbeli switch-en]]
- [[4.3.4 A 3. rétegbeli switch VLAN-ok közötti forgalomirányításának ellenőrzése]]

### Címkék

#irányított-port #OSPF #ip-routing #show-ip-route #3-réteg #konfiguráció #Cisco

---
